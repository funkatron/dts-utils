# 2026-05-02 — release 0.3.0 + CI

- **CHANGELOG:** `[Unreleased]` promoted to **`[0.3.0] - 2026-05-02`** with **Tested with** (pytest counts; no gRPC smoke). Fresh empty `[Unreleased]`. Example block under “Documenting gRPCServerCLI” uses placeholder **`[0.4.0]`**.
- **`pyproject.toml`:** version **0.3.0**; **uv.lock** updated.
- **`.github/workflows/ci.yml`:** `ubuntu-latest`, `astral-sh/setup-uv`, `uv sync --dev`, `uv run pytest -q` on **`push`/`pull_request`** to **`main`**.
- **Git:** annotated tag **`v0.3.0`** pushed with **`main`**.

Prior state: **no** repo-local CI; tests were not running on GitHub automatically.
